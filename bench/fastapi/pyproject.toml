[project]
name = "unique-bench-fastapi"
version = "1.0.0"
description = "FastAPI equivalent of the unique bench, for apples-to-apples comparison"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[tool.uvicorn]
host = "127.0.0.1"
port = 3003
